from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Prediction of the quality of the wine using data_given prediction using mlops',
    author='trinadh',
    license='MIT',
)
